{
  "name": "nextjs",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "targets": {
    "docker": {
      "executor": "nx:run-commands",
      "dependsOn": [
        "build"
      ],
      "options": {
        "commands": [
          "docker build -t experiment-nx-nextjs -f apps/nextjs/Dockerfile ."
        ]
      }
    }
  }
}